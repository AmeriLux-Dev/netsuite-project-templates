{
  "template": "{{projectType}}",
  "templateRef": "{{templateRef}}",
  "appName": "{{appName}}",
  "prefix": "{{prefix}}",
  "cliVersion": "{{cliVersion}}",
  "features": {
    "performanceTracker": {{performanceTrackerJson}},
    "probity": {{probityJson}}
  }
}
