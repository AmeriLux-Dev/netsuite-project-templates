{
  "name": "{{appNameKebab}}-common",
  "version": "0.1.0",
  "private": true,
  "description": "Types and NetSuite identifiers shared by the api and client workspaces.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@amerilux/netsuite-repository": "^1.1.0"
  }
}
