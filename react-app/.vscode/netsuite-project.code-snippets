{
    "nspControllerRestlet": {
        "scope": "typescript",
        "prefix": "nspControllerRestlet",
        "description": "api/src/controllers/<name>Controller.ts as a Restlet: shapes, endpoints, script declaration; name and ids come from the file name. Next: nspObjectRestlet in netsuite/Objects, npm run generate, then nspHookQueryWith.",
        "body": [
            "/**",
            " * @NApiVersion 2.1",
            " * @NScriptType Restlet",
            " * @NModuleScope SameAccount",
            " */",
            "",
            "import { defineEndpoints, defineRestlet } from '@amerilux/netsuite-api/server';",
            "import { ${2:serviceFunction}, type ${3:ModelSummary} } from '../services/${4:${TM_FILENAME_BASE/(.*)Controller$/$1/}}Service';",
            "",
            "/**",
            " * The ${TM_FILENAME_BASE/(.*)Controller$/$1/} controller: ${8:what it serves}. The shapes are the wire, not the record. An endpoint",
            " * unpacks the request, calls a service with plain arguments and shapes the reply field by field.",
            " */",
            "",
            "export interface ${1/^(.)/${1:/upcase}/}Request {",
            "    ${5:id}: ${6:number};",
            "}",
            "",
            "export interface ${1/^(.)/${1:/upcase}/}Response {",
            "    ${5}: ${6};",
            "    ${7:items}: ${3}[];",
            "}",
            "",
            "export const ${TM_FILENAME_BASE/(.*)Controller$/$1/}Endpoints = defineEndpoints({",
            "    ${1:list}: (request: ${1/^(.)/${1:/upcase}/}Request): ${1/^(.)/${1:/upcase}/}Response => {",
            "        const ${5} = request.${5};",
            "        return { ${5}, ${7}: ${2}(${5}) };",
            "    },",
            "});",
            "",
            "/** The endpoint signatures as a type, for server code that calls this controller through the Suitelet client. */",
            "export type ${TM_FILENAME_BASE/^(.)(.*)Controller$/${1:/upcase}$2/}Endpoints = typeof ${TM_FILENAME_BASE/(.*)Controller$/$1/}Endpoints;",
            "",
            "export const post = defineRestlet({",
            "    name: '${TM_FILENAME_BASE/(.*)Controller$/$1/}',",
            "    scriptId: 'customscript_{{prefix}}_${TM_FILENAME_BASE/(?:Controller$)|([A-Z])/${1:+_}${1:/downcase}/g}',",
            "    deployId: 'customdeploy_{{prefix}}_${TM_FILENAME_BASE/(?:Controller$)|([A-Z])/${1:+_}${1:/downcase}/g}',",
            "}, ${TM_FILENAME_BASE/(.*)Controller$/$1/}Endpoints);",
            ""
        ]
    },
    "nspControllerSuitelet": {
        "scope": "typescript",
        "prefix": "nspControllerSuitelet",
        "description": "api/src/controllers/<name>Controller.ts as a Suitelet; name and ids come from the file name. Drop `browser: false` when the browser calls it. Next: nspObjectSuitelet in netsuite/Objects, npm run generate, then nspRepositorySuitelet or nspHookQueryWith.",
        "body": [
            "/**",
            " * @NApiVersion 2.1",
            " * @NScriptType Suitelet",
            " * @NModuleScope SameAccount",
            " */",
            "",
            "import { defineEndpoints, defineSuitelet } from '@amerilux/netsuite-api/server';",
            "import { ${2:serviceFunction}, type ${3:ModelSummary} } from '../services/${4:${TM_FILENAME_BASE/(.*)Controller$/$1/}}Service';",
            "",
            "/**",
            " * The ${TM_FILENAME_BASE/(.*)Controller$/$1/} controller: ${8:what it serves}, and why a Suitelet. The shapes are the wire, not the record.",
            " * An endpoint unpacks the request, calls a service with plain arguments and shapes the reply field by field.",
            " */",
            "",
            "export interface ${1/^(.)/${1:/upcase}/}Request {",
            "    ${5:id}: ${6:number};",
            "}",
            "",
            "export interface ${1/^(.)/${1:/upcase}/}Response {",
            "    ${5}: ${6};",
            "    ${7:items}: ${3}[];",
            "}",
            "",
            "export const ${TM_FILENAME_BASE/(.*)Controller$/$1/}Endpoints = defineEndpoints({",
            "    ${1:list}: (request: ${1/^(.)/${1:/upcase}/}Request): ${1/^(.)/${1:/upcase}/}Response => {",
            "        const ${5} = request.${5};",
            "        return { ${5}, ${7}: ${2}(${5}) };",
            "    },",
            "});",
            "",
            "/** What a Suitelet client of this script is built from: `createSuiteletClient<${TM_FILENAME_BASE/^(.)(.*)Controller$/${1:/upcase}$2/}Endpoints>(scripts.${TM_FILENAME_BASE/(.*)Controller$/$1/})`. */",
            "export type ${TM_FILENAME_BASE/^(.)(.*)Controller$/${1:/upcase}$2/}Endpoints = typeof ${TM_FILENAME_BASE/(.*)Controller$/$1/}Endpoints;",
            "",
            "export const onRequest = defineSuitelet({",
            "    name: '${TM_FILENAME_BASE/(.*)Controller$/$1/}',",
            "    scriptId: 'customscript_{{prefix}}_${TM_FILENAME_BASE/(?:Controller$)|([A-Z])/${1:+_}${1:/downcase}/g}',",
            "    deployId: 'customdeploy_{{prefix}}_${TM_FILENAME_BASE/(?:Controller$)|([A-Z])/${1:+_}${1:/downcase}/g}',",
            "    ${9:browser: false,}",
            "}, ${TM_FILENAME_BASE/(.*)Controller$/$1/}Endpoints);",
            ""
        ]
    },
    "nspControllerShapes": {
        "scope": "typescript",
        "prefix": "nspControllerShapes",
        "description": "Request and response shapes for one more endpoint, above defineEndpoints. Every type in a controller is exported. Then nspControllerEndpoint inside defineEndpoints.",
        "body": [
            "export interface ${1:ById}Request {",
            "    ${2:id}: ${3:number};",
            "}",
            "",
            "export interface ${1}Response {",
            "    ${2}: ${3};",
            "    ${4:items}: ${5:ModelSummary}[];",
            "}",
            ""
        ]
    },
    "nspControllerEndpoint": {
        "scope": "typescript",
        "prefix": "nspControllerEndpoint",
        "description": "One more endpoint inside defineEndpoints: unpack the request, call the service with plain arguments, shape the response. Its shapes come from nspControllerShapes.",
        "body": [
            "/** ${7:What it answers}. */",
            "${1:byId}: (request: ${2:${1/^(.)/${1:/upcase}/}Request}): ${3:${1/^(.)/${1:/upcase}/}Response} => {",
            "    const ${4:id} = request.${4};",
            "    return { ${4}, ${5:items}: ${6:serviceFunction}(${4}) };",
            "},"
        ]
    },
    "nspControllerParse": {
        "scope": "typescript",
        "prefix": "nspControllerParse",
        "description": "Guard for an id that comes off the wire, above defineEndpoints: a 400 instead of a query for NaN. Add ApiError to the import from @amerilux/netsuite-api/server and call parse<Id>(request.<id>) in the handler.",
        "body": [
            "/** The wire promises a number; a caller that sends something else gets a 400, not a query for NaN. */",
            "function parse${1/^(.)/${1:/upcase}/}(requested: number | string | undefined): number {",
            "    const parsed = typeof requested === 'string' ? Number.parseInt(requested, 10) : requested;",
            "    if (parsed === undefined || Number.isNaN(parsed) || parsed <= 0) {",
            "        throw ApiError.badRequest('${1:id} must be a positive whole number.', { ${1}: requested });",
            "    }",
            "    return parsed;",
            "}",
            ""
        ]
    },
    "nspControllerAuthorize": {
        "scope": "typescript",
        "prefix": "nspControllerAuthorize",
        "description": "Options for the define call, as its third argument after the endpoints: `}, <name>Endpoints, { authorize });`. Runs before every handler. Add ApiError to the import.",
        "body": [
            "{",
            "    // Runs before every handler; throw to reject. Read the session through a repository function.",
            "    authorize: ({ endpoint, request }) => {",
            "        if (${1:!isAllowed(endpoint, request)}) throw ApiError.forbidden();",
            "    },",
            "}"
        ]
    },
    "nspJob": {
        "scope": "typescript",
        "prefix": "nspJob",
        "description": "api/src/jobs/<name>/<name>.ts: the file NetSuite loads for a job. Carries the script's header and says which stages there are. Next: nspJobIds in netsuite.ts, nspJobGetInputData, nspJobMap, nspJobSummarize and nspJobStart in the same folder, nspObjectMapReduce in netsuite/Objects, then npm run generate.",
        "body": [
            "/**",
            " * @NApiVersion 2.1",
            " * @NScriptType MapReduceScript",
            " * @NModuleScope SameAccount",
            " */",
            "",
            "/**",
            " * The ${TM_FILENAME_BASE} job: ${1:what a run of it does}.",
            " *",
            " * This is the file NetSuite loads: it says which stages there are, and each stage file beside it holds",
            " * what that stage does, with the shapes on either end of a run declared where the stage that names them",
            " * is. The job's ids are in netsuite.ts, under `jobs`. Nothing imports this file.",
            " */",
            "",
            "export { getInputData } from './getInputData';",
            "export { map } from './map';",
            "export { summarize } from './summarize';",
            ""
        ]
    },
    "nspJobIds": {
        "scope": "typescript",
        "prefix": "nspJobIds",
        "description": "One job's entry in the `jobs` object of netsuite.ts: the ids its SDF object gives it. A stage passes the entry to openJobRun and closeJobRun, and a start passes it to startJobRun.",
        "body": [
            "${1:closeOldOrders}: {",
            "    name: '${1}',",
            "    scriptId: 'customscript_{{prefix}}_${2:close_old_orders}_mr',",
            "    deployments: ['customdeploy_{{prefix}}_${2}_mr'],",
            "    runParameter: 'custscript_{{prefix}}_${2}_run',",
            "},"
        ]
    },
    "nspJobGetInputData": {
        "scope": "typescript",
        "prefix": "nspJobGetInputData",
        "description": "api/src/jobs/<job>/getInputData.ts: the stage that opens the run and answers the work, with the shapes a run is started with and works through. The generator reads the run's input from the type on openJobRun.",
        "body": [
            "import type { EntryPoints } from 'N/types';",
            "import { jobs } from '../../../../netsuite';",
            "import { openJobRun } from '../../repositories/jobRunRepository';",
            "import { ${1:listWork} } from '../../${2|services,repositories|}/${3:subject}${4|Service,Repository|}';",
            "",
            "/** What a run of this job is asked to do; whatever starts a run builds this. */",
            "export interface ${5:WorkRequest} {",
            "    ${6:olderThanDays}: ${7:number};",
            "}",
            "",
            "/** One piece of the work: what the map stage is handed. */",
            "export interface ${8:WorkItem} {",
            "    ${9:orderId}: number;",
            "}",
            "",
            "/** ${10:What the work is, and in what order.} */",
            "export function getInputData(_context: EntryPoints.MapReduce.getInputDataContext): ${8}[] {",
            "    const input = openJobRun<${5}>(jobs.${11:closeOldOrders});",
            "    return ${1}(input.${6}).map((${9}) => ({ ${9} }));",
            "}",
            ""
        ]
    },
    "nspJobMap": {
        "scope": "typescript",
        "prefix": "nspJobMap",
        "description": "api/src/jobs/<job>/map.ts: the stage that does one item and writes what it came to, as NetSuite calls it. A test calls it with mapContextFor and reads what it wrote.",
        "body": [
            "import type { EntryPoints } from 'N/types';",
            "import { ${1:doOneThing} } from '../../${2|services,repositories|}/${3:subject}${4|Service,Repository|}';",
            "import type { ${5:WorkItem} } from './getInputData';",
            "",
            "/** What the map stage writes per item, and what summarize gathers. */",
            "export interface ${6:WorkOutcome} {",
            "    ${7:orderId}: number;",
            "    ${8:closed}: boolean;",
            "}",
            "",
            "/** ${9:What one item does}; a failure here is one item, not the run: NetSuite collects it and the run carries it. */",
            "export function map(context: EntryPoints.MapReduce.mapContext): void {",
            "    const item = JSON.parse(context.value) as ${5};",
            "    const outcome: ${6} = { ${7}: item.${7}, ${8}: ${1}(item.${7}) };",
            "    context.write({ key: String(item.${7}), value: JSON.stringify(outcome) });",
            "}",
            ""
        ]
    },
    "nspJobSummarize": {
        "scope": "typescript",
        "prefix": "nspJobSummarize",
        "description": "api/src/jobs/<job>/summarize.ts: the stage that builds the result and closes the run. The generator reads the run's result from the type on closeJobRun.",
        "body": [
            "import type { EntryPoints } from 'N/types';",
            "import { jobs } from '../../../../netsuite';",
            "import { closeJobRun } from '../../repositories/jobRunRepository';",
            "import type { ${1:WorkOutcome} } from './map';",
            "",
            "/** What a finished run leaves behind; the page reads it as jobs.<name>.Result. */",
            "export interface ${2:WorkResult} {",
            "    ${3:handled}: number;",
            "    ${4:outcomes}: ${1}[];",
            "}",
            "",
            "export function summarize(context: EntryPoints.MapReduce.summarizeContext): void {",
            "    const ${4}: ${1}[] = [];",
            "    context.output.iterator().each((_key, value) => {",
            "        ${4}.push(JSON.parse(value) as ${1});",
            "        return true;",
            "    });",
            "    closeJobRun<${2}>(jobs.${5:closeOldOrders}, context, { ${3}: ${4}.length, ${4} });",
            "}",
            ""
        ]
    },
    "nspJobStart": {
        "scope": "typescript",
        "prefix": "nspJobStart",
        "description": "api/src/jobs/<job>/start.ts: how a run of the job is started. The one file of the folder that is not a stage, and the one a controller calls.",
        "body": [
            "import { jobs } from '../../../../netsuite';",
            "import { startJobRun } from '../../repositories/jobRunRepository';",
            "import type { ${1:WorkRequest} } from './getInputData';",
            "",
            "/**",
            " * Starts a run of this job and answers its id; the page follows the run by that id. Throws a 409 when every",
            " * deployment of the job is already running, which the page can offer to retry.",
            " */",
            "export function start${2:ClosingOldOrders}(${3:olderThanDays}: ${4:number}): string {",
            "    return startJobRun(jobs.${5:closeOldOrders}, { ${3} } satisfies ${1});",
            "}",
            ""
        ]
    },
    "nspJobReduce": {
        "scope": "typescript",
        "prefix": "nspJobReduce",
        "description": "api/src/jobs/<job>/reduce.ts: the stage that gathers what map wrote under one key. Export it from the job's file as well, beside the other stages.",
        "body": [
            "import type { EntryPoints } from 'N/types';",
            "import type { ${1:WorkOutcome} } from './map';",
            "",
            "/** ${2:What one key comes to} once every item under it has been written. */",
            "export function reduce(context: EntryPoints.MapReduce.reduceContext): void {",
            "    const ${3:outcomes} = context.values.map((value) => JSON.parse(value) as ${1});",
            "    context.write({ key: context.key, value: JSON.stringify(${4:${3}.length}) });",
            "}",
            ""
        ]
    },
    "nspJobParameter": {
        "scope": "typescript",
        "prefix": "nspJobParameter",
        "description": "One more script parameter on a job's entry in netsuite.ts. Add the matching <scriptcustomfield> to the job's SDF object and a value on the deployment; a repository function reads it, as it reads anything else NetSuite holds.",
        "body": [
            "parameters: { ${1:batchSize}: 'custscript_{{prefix}}_${2:batch_size}' },"
        ]
    },
    "nspUserEvent": {
        "scope": "typescript",
        "prefix": "nspUserEvent",
        "description": "api/src/events/user/<subject>.ts: a self-contained user event. Reads and writes the record its context carries; anything else in NetSuite goes through a repository. Create its script record and deployments in NetSuite yourself.",
        "body": [
            "/**",
            " * @NApiVersion 2.1",
            " * @NScriptType UserEventScript",
            " * @NModuleScope SameAccount",
            " */",
            "",
            "import * as log from 'N/log';",
            "import type { EntryPoints } from 'N/types';",
            "import { ${4:readSomethingRelated} } from '../../repositories/${5:subject}Repository';",
            "",
            "/**",
            " * ${6:What this does and when}. Self-contained on purpose: it belongs to the record, not to this",
            " * application, so it works the record its context carries and reaches anything else through a",
            " * repository function. It never blocks a save for something that is not the user's doing: the work",
            " * is in a try/catch that logs.",
            " *",
            " * Deployed by hand: create the script record and its deployments in NetSuite (record type ${1:Sales Order},",
            " * the contexts it should run in), pointing at /SuiteScripts/{{appName}}/api/events/user/${TM_FILENAME_BASE}.js.",
            " */",
            "",
            "/** The ids this event works with, written here because the event is its own. */",
            "const fields = {",
            "    ${2:memo}: '${3:memo}',",
            "} as const;",
            "",
            "export const beforeSubmit: EntryPoints.UserEvent.beforeSubmit = (context: EntryPoints.UserEvent.beforeSubmitContext): void => {",
            "    if (context.type !== context.UserEventType.CREATE && context.type !== context.UserEventType.EDIT) return;",
            "    try {",
            "        const related = ${4}(Number(context.newRecord.id));",
            "        context.newRecord.setValue({ fieldId: fields.${2}, value: String(related) });",
            "        log.debug('event applied', { record: context.newRecord.type, id: context.newRecord.id });",
            "    } catch (error) {",
            "        log.error('event failed', { record: context.newRecord.type, id: context.newRecord.id, message: error instanceof Error ? error.message : String(error) });",
            "    }",
            "};",
            ""
        ]
    },
    "nspClientEvent": {
        "scope": "typescript",
        "prefix": "nspClientEvent",
        "description": "api/src/events/client/<subject>.ts: a client script for a record page. Runs in the browser with N/* and console; no telemetry and no part of this application. Attach it in NetSuite (a deployment, or a user event's beforeLoad).",
        "body": [
            "/**",
            " * @NApiVersion 2.1",
            " * @NScriptType ClientScript",
            " * @NModuleScope SameAccount",
            " */",
            "",
            "import * as currentRecord from 'N/currentRecord';",
            "import type { EntryPoints } from 'N/types';",
            "",
            "/**",
            " * ${4:What this does on the page}. A page script: it runs in the browser on a ${1:sales order} form, so it",
            " * calls N/* itself and logs with console. Nothing of this application belongs here.",
            " *",
            " * Attached by hand: deploy it to the record type in NetSuite, or set it as the form's client script",
            " * from a user event's beforeLoad, pointing at /SuiteScripts/{{appName}}/api/events/client/${TM_FILENAME_BASE}.js.",
            " */",
            "",
            "const fields = {",
            "    ${2:quantity}: '${3:quantity}',",
            "} as const;",
            "",
            "export const fieldChanged: EntryPoints.Client.fieldChanged = (context: EntryPoints.Client.fieldChangedContext): void => {",
            "    if (context.fieldId !== fields.${2}) return;",
            "    const record = currentRecord.get();",
            "    const value = Number(record.getValue({ fieldId: fields.${2} }));",
            "    if (Number.isNaN(value) || value <= 0) console.warn(fields.${2} + ' is not a positive number');",
            "};",
            ""
        ]
    },
    "nspService": {
        "scope": "typescript",
        "prefix": "nspService",
        "description": "api/src/services/<subject>Service.ts: plain arguments in, a type the service declares out, built by build<Model>Summary. Nothing in a service names a controller. Next: nspRepository for the repository it calls, nspTestService.",
        "body": [
            "import type { ${1:${TM_FILENAME_BASE/^(.)|(ies)Service$|(ss|x|z|ch|sh)esService$|s?Service$/${1:/upcase}${2:+y}$3/g}} } from '../types/models.gen';",
            "import { ${2:repositoryFunction} } from '../repositories/${3:${TM_FILENAME_BASE/(.*)Service$/$1/}}Repository';",
            "",
            "/**",
            " * Decisions about ${4:the subject}. The service takes plain arguments and returns types it declares",
            " * itself: it knows nothing about the wire, so any controller can call it and shape the reply its own way.",
            " */",
            "",
            "/** A ${1} as the service hands it up: what a caller may see, nothing more. */",
            "export type ${1}Summary = Pick<${1}, '${5:id}'>;",
            "",
            "export function build${1}Summary(${6:${1/^(.)/${1:/downcase}/}}: ${1}): ${1}Summary {",
            "    return { ${5}: ${6}.${5} };",
            "}",
            "",
            "/** ${7:What it answers}. */",
            "export function ${8:serviceFunction}(${9:id}: ${10:number}): ${1}Summary[] {",
            "    return ${2}(${9}).map(build${1}Summary);",
            "}",
            ""
        ]
    },
    "nspRepository": {
        "scope": "typescript",
        "prefix": [
            "nspRepository",
            "nspRepo"
        ],
        "description": "api/src/repositories/<set>Repository.ts reading through dbContext with a specification; set and model names come from the file name. Next: nspSpecification, nspRepositoryCreate / nspRepositoryUpdate for writes, nspTestRepository.",
        "body": [
            "import type { ${1:${TM_FILENAME_BASE/^(.)|(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:/upcase}${2:+y}$3/g}} } from '../types/models.gen';",
            "import { dbContext } from './generated/context.gen';",
            "import { ${3:byId} } from '../specifications/${2:${TM_FILENAME_BASE/(.*)Repository$/$1/}}Specifications';",
            "",
            "/**",
            " * Data access for ${4:the subject}: sentences built from the specifications over dbContext. A read goes",
            " * through the set and tracks nothing; a write calls dbContext.withTracking() and finishes before returning.",
            " */",
            "",
            "/** ${5:What it lists}. */",
            "export function ${8:list${2/^(.)/${1:/upcase}/}By${6/^(.)/${1:/upcase}/}}(${6:id}: ${7:number}): ${1}[] {",
            "    return dbContext.${2}.list(${3}(${6}));",
            "}",
            ""
        ]
    },
    "nspRepositoryCreate": {
        "scope": "typescript",
        "prefix": "nspRepositoryCreate",
        "description": "One more function in a dbContext repository: a create through withTracking(). Add <Model>Create to the import from ../types/models.gen.",
        "body": [
            "/** ${1:Creates one} and returns it with its new id. The tracker lives only inside this function. */",
            "export function create${TM_FILENAME_BASE/^(.)|(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:/upcase}${2:+y}$3/g}(fields: ${TM_FILENAME_BASE/^(.)|(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:/upcase}${2:+y}$3/g}Create): ${TM_FILENAME_BASE/^(.)|(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:/upcase}${2:+y}$3/g} {",
            "    return dbContext.withTracking().${TM_FILENAME_BASE/(.*)Repository$/$1/}.create(fields);",
            "}",
            ""
        ]
    },
    "nspRepositoryUpdate": {
        "scope": "typescript",
        "prefix": "nspRepositoryUpdate",
        "description": "One more function in a dbContext repository: a one-call update through withTracking() (load, patch, save; throws on failure). Add <Model>Patch to the import from ../types/models.gen. Narrow the name to what changes.",
        "body": [
            "/** ${1:What changes}. */",
            "export function ${2:update}${TM_FILENAME_BASE/^(.)|(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:/upcase}${2:+y}$3/g}(${3:${TM_FILENAME_BASE/(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:+y}$2Id/g}}: number, patch: ${TM_FILENAME_BASE/^(.)|(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:/upcase}${2:+y}$3/g}Patch): ${TM_FILENAME_BASE/^(.)|(ies)Repository$|(ss|x|z|ch|sh)esRepository$|s?Repository$/${1:/upcase}${2:+y}$3/g} {",
            "    return dbContext.withTracking().${TM_FILENAME_BASE/(.*)Repository$/$1/}.update(${3}, patch);",
            "}",
            ""
        ]
    },
    "nspRepositorySuitelet": {
        "scope": "typescript",
        "prefix": "nspRepositorySuitelet",
        "description": "api/src/repositories/<name>Repository.ts calling another controller of this application through its Suitelet client; the controller name comes from the file name and its types are imported as types only. Next: nspTestRepositorySuitelet.",
        "body": [
            "import { createSuiteletClient } from '@amerilux/netsuite-api/server';",
            "import type { ${1:ByIdResponse}, ${2:${TM_FILENAME_BASE/^(.)(.*)Repository$/${1:/upcase}$2/}Endpoints} } from '../controllers/${3:${TM_FILENAME_BASE/(.*)Repository$/$1/}}Controller';",
            "import { scripts } from '../scripts.gen';",
            "",
            "/**",
            " * ${4:What this reads}, read through the ${3} Suitelet: data access, so a repository; the service does not know",
            " * the answer came from another script. What comes back is that script's wire shape, so the return type is",
            " * taken from its controller (as a type only).",
            " */",
            "",
            "const ${3}Api = createSuiteletClient<${2}>(scripts.${3});",
            "",
            "export function ${5:listItemsFor}(${6:id}: ${7:number}): ${1}['${8:items}'] {",
            "    return ${3}Api.${9:byId}({ ${6} }).${8};",
            "}",
            ""
        ]
    },
    "nspRepositoryModule": {
        "scope": "typescript",
        "prefix": "nspRepositoryModule",
        "description": "api/src/repositories/<source>Repository.ts reading a NetSuite module (N/runtime, N/file, ...): a repository, because only a repository touches N/*. Returns a type of its own.",
        "body": [
            "import * as ${1:runtime} from 'N/${1}';",
            "",
            "/**",
            " * Data access for ${2:what this reads}: a repository rather than a service concern, because it reads a",
            " * NetSuite module; the service composes what it returns with other repository answers.",
            " */",
            "",
            "export interface ${3:Result} {",
            "    ${4:id}: ${5:number};",
            "}",
            "",
            "export function ${6:read${3}}(): ${3} {",
            "    const ${7:value} = ${1}.${8:getCurrentUser()};",
            "    return { ${4}: ${9:Number(${7}.${4})} };",
            "}",
            ""
        ]
    },
    "nspSpecification": {
        "scope": "typescript",
        "prefix": [
            "nspSpecification",
            "nspSpec"
        ],
        "description": "api/src/specifications/<set>Specifications.ts: one predicate per builder, no decisions. The model name is inferred from the file name; type over it if the guess is wrong.",
        "body": [
            "import type { Specification } from '@amerilux/netsuite-repository';",
            "import { type ${1:${TM_FILENAME_BASE/^(.)|(ies)Specifications$|(ss|x|z|ch|sh)esSpecifications$|s?Specifications$/${1:/upcase}${2:+y}$3/g}}, ${1}Fields as Fields } from '../repositories/generated/${1}.gen';",
            "",
            "/** The query vocabulary for ${6:the subject}: one predicate per builder. A repository function composes them. */",
            "",
            "export const ${2:byId} = (${3:id}: ${4:number}): Specification<${1}> =>",
            "    (query) => query.where(Fields.${5:id}, '=', ${3});",
            ""
        ]
    },
    "nspModel": {
        "scope": "typescript",
        "prefix": "nspModel",
        "description": "api/src/models/<Record>.ts: the model class named from the file name; the property `id` is the internal id. NetsuiteRecordType lists the native record types; a custom record uses its id string. nspHelpModel shows every decorator once. Then npm run generate.",
        "body": [
            "import { Field, NetsuiteRecordType, RecordType } from '@amerilux/netsuite-repository';",
            "",
            "/**",
            " * ${5:What one row is}. The record's type and field ids are declared here and nowhere else.",
            " * Run `npm run generate` after editing: it refreshes api/src/repositories/generated/ and api/src/types/models.gen.ts.",
            " */",
            "@RecordType(${1:NetsuiteRecordType.SALES_ORDER})",
            "export class ${TM_FILENAME_BASE} {",
            "    id!: number;",
            "",
            "    @Field('${2:tranid}')",
            "    ${3:tranId}!: ${4:string};",
            "}",
            ""
        ]
    },
    "nspHelpModel": {
        "scope": "typescript",
        "prefix": "nspHelpModel",
        "description": "Help: api/src/models/<Record>.ts with every decorator once, each with a comment, so the options are in view. Keep what the record needs, delete the rest. Includes a subrecord class and a sublist line class in the same file. Then npm run generate.",
        "body": [
            "import { ExcludeFromDefaultSelect, Field, InternalId, NetsuiteRecordType, NotMapped, ParentId, ReadOnly, RecordType, SetFirst, Sublist, Subrecord, Transform } from '@amerilux/netsuite-repository';",
            "import type { ${1:Customer} } from './${1}';",
            "",
            "/**",
            " * ${TM_FILENAME_BASE}, with every decorator once as a starting point: keep what the record needs, delete the rest.",
            " * Three rules cover most of a model. Every declared property is a mapped field (@NotMapped opts out). Every body",
            " * field is writable (@ReadOnly opts out); its field id is the lowercased property name, and @Field renames it. A",
            " * property typed as another model class is a reference, a subrecord or a sublist, and the join comes from the",
            " * declared type. Run `npm run generate` after editing.",
            " */",
            "",
            "/** A transform runs on every value read. It must be an exported function: the build step imports it by name. */",
            "export function ${2:trimText}(value: unknown): string {",
            "    return String(value ?? '').trim();",
            "}",
            "",
            "/** A subrecord is a plain class without @RecordType; the field id comes from the property that uses it. */",
            "export class ${TM_FILENAME_BASE}Address {",
            "    addr1!: string | null;",
            "",
            "    city!: string | null;",
            "",
            "    /** Written before the other fields of the subrecord, because NetSuite recomputes dependants when it changes. */",
            "    @SetFirst()",
            "    state!: string | null;",
            "",
            "    zip!: string | null;",
            "}",
            "",
            "/** A sublist line is a record type like any other; what differs is the key (`line`, queried as `id`) and @ParentId(), the field N/query joins the lines through. */",
            "@RecordType('${3:transactionline}')",
            "export class ${TM_FILENAME_BASE}Line {",
            "    /** The internal id when it is not the property `id`: queried as `id`, written through the sublist field `line`. */",
            "    @InternalId()",
            "    @Field('line', { queryFieldId: 'id' })",
            "    lineId!: number;",
            "",
            "    @ParentId()",
            "    @Field('transaction')",
            "    @ReadOnly()",
            "    ${TM_FILENAME_BASE/^(.)/${1:/downcase}/}Id!: number;",
            "",
            "    @Field('item')",
            "    itemId!: number;",
            "",
            "    quantity!: number;",
            "",
            "    @ReadOnly()",
            "    amount!: number;",
            "}",
            "",
            "@RecordType(${4:NetsuiteRecordType.INVOICE}, {",
            "    // queryType: 'transaction',   the N/query root when it is not the record type",
            "    // filter: [{ fieldId: 'mainline', operator: 'IS', values: [true] }],   conditions applied to every query",
            "    // setName: 'documents',   the set on dbContext; the default is the pluralized class name",
            "    // coerce: true,   coerce query values to the declared types",
            "    // updater: { ... },   default RecordUpdaterOptions for every write",
            "})",
            "export class ${TM_FILENAME_BASE} {",
            "    /** The internal id: the property `id`, read-only, by convention. */",
            "    id!: number;",
            "",
            "    /** @Field renames a field whose id is not the lowercased property name. */",
            "    @Field('tranid')",
            "    tranId!: string;",
            "",
            "    /** The type comes from the property: string, number, boolean, Date; string[] or number[] for a multiselect. */",
            "    tranDate!: Date;",
            "",
            "    memo?: string | null;",
            "",
            "    /** A select field with no reference behind it: N/query compares select and key fields through ANY_OF, so the type is declared. */",
            "    @Field('location', { type: 'select' })",
            "    locationId!: number | null;",
            "",
            "    /** Queried under one field id, written under another. */",
            "    @Field('orderstatus', { queryFieldId: 'status' })",
            "    status!: string;",
            "",
            "    /** The display text of a select field, read-only; the select field itself is the property above. */",
            "    @Field({ queryFieldId: 'status', text: true })",
            "    statusText!: string;",
            "",
            "    /** A reference: the select field behind it is `<reference>Id`, and the Pick is the projection, so only those fields are read. */",
            "    /** @Reference() is needed only to name another select property, a targetKey, or load: 'separate' for a join N/query lacks. */",
            "    @Field('${5:entity}')",
            "    ${6:customer}Id!: number;",
            "    ${6}?: Pick<${1}, '${7:id}' | '${8:companyName}'>;",
            "",
            "    /** Excluded from writes. */",
            "    @Field('foreigntotal')",
            "    @ReadOnly()",
            "    total!: number;",
            "",
            "    /** Left out of the default select; include() brings it back for one query. */",
            "    @ExcludeFromDefaultSelect()",
            "    @Field('custbody_notes')",
            "    notes!: string | null;",
            "",
            "    /** Transformed on every read. */",
            "    @Transform(${2})",
            "    @Field('otherrefnum')",
            "    poNumber!: string | null;",
            "",
            "    /** Not a field: stays on the type for a value filled in after the query. */",
            "    @NotMapped()",
            "    displayLabel?: string;",
            "",
            "    /** A subrecord; clearListField names the list field cleared before an edit. */",
            "    @Subrecord({ clearListField: 'shipaddresslist' })",
            "    shippingAddress!: ${TM_FILENAME_BASE}Address;",
            "",
            "    /** The lines, reached from the transaction root through transactionlines, without the header line. */",
            "    @Sublist('item', { queryType: 'transaction', relationship: 'transactionlines', filter: [{ fieldId: 'mainline', operator: 'IS', values: [false] }] })",
            "    lines!: ${TM_FILENAME_BASE}Line[];",
            "}",
            ""
        ]
    },
    "nspModelSubrecordClass": {
        "scope": "typescript",
        "prefix": "nspModelSubrecordClass",
        "description": "api/src/models/<Owner>Address.ts: a subrecord class, named from the file name. A plain class without @RecordType is what makes the owner's property a subrecord (a record class would be read as a reference); a subrecord with a table of its own carries @RecordType and its own @InternalId(), and the owner then marks the property with @Subrecord() explicitly. The owner uses it through nspModelSubrecord.",
        "body": [
            "import { SetFirst } from '@amerilux/netsuite-repository';",
            "",
            "/** ${1:What the subrecord holds}: a plain class without @RecordType; the field id comes from the property that uses it. */",
            "export class ${TM_FILENAME_BASE} {",
            "    ${2:addr1}!: ${3:string | null};",
            "",
            "    /** Written before the other fields, because NetSuite recomputes dependants when it changes. */",
            "    @SetFirst()",
            "    ${4:state}!: string | null;",
            "}",
            ""
        ]
    },
    "nspModelBase": {
        "scope": "typescript",
        "prefix": "nspModelBase",
        "description": "api/src/models/<Name>.ts: an abstract mapping base without @RecordType, named from the file name. Each record type that extends it queries its own type and inherits these fields.",
        "body": [
            "import { Field } from '@amerilux/netsuite-repository';",
            "",
            "/** Common fields of ${1:every transaction}. No @RecordType, so no record set of its own: each record type that extends it queries its own type and inherits these. */",
            "export abstract class ${TM_FILENAME_BASE} {",
            "    id!: number;",
            "",
            "    @Field('${2:tranid}')",
            "    ${3:tranId}!: string;",
            "}",
            ""
        ]
    },
    "nspModelField": {
        "scope": "typescript",
        "prefix": "nspModelField",
        "description": "One more field on a model. The field id defaults to the lowercased property name; @Field renames it.",
        "body": [
            "@Field('${1:fieldid}')",
            "${2:property}!: ${3:string};"
        ]
    },
    "nspModelFieldText": {
        "scope": "typescript",
        "prefix": "nspModelFieldText",
        "description": "One more property: the display text of a select field, read-only. A second property holds the select field itself.",
        "body": [
            "/** The display text of the ${1:status} select field, read-only; a second property holds the select field itself. */",
            "@Field({ queryFieldId: '${1}', text: true })",
            "${2:${1}Text}!: string;"
        ]
    },
    "nspModelFieldSelect": {
        "scope": "typescript",
        "prefix": "nspModelFieldSelect",
        "description": "One more property: a select field with no reference behind it. N/query compares select and key fields through ANY_OF, so the type is declared.",
        "body": [
            "/** A select field with no reference behind it: N/query compares it through ANY_OF, so the type is declared. */",
            "@Field('${1:location}', { type: 'select' })",
            "${2:${1}Id}!: number | null;"
        ]
    },
    "nspModelFieldSplit": {
        "scope": "typescript",
        "prefix": "nspModelFieldSplit",
        "description": "One more property: a field NetSuite queries under one id and writes under another.",
        "body": [
            "/** Queried as ${2} and written as ${1}. */",
            "@Field('${1:orderstatus}', { queryFieldId: '${2:status}' })",
            "${3:${2}}!: string;"
        ]
    },
    "nspModelReadOnly": {
        "scope": "typescript",
        "prefix": "nspModelReadOnly",
        "description": "One more property, excluded from writes. Add ReadOnly to the import.",
        "body": [
            "@Field('${1:foreigntotal}')",
            "@ReadOnly()",
            "${2:total}!: ${3:number};"
        ]
    },
    "nspModelInternalId": {
        "scope": "typescript",
        "prefix": "nspModelInternalId",
        "description": "The internal id when it is not the property `id` (a sublist line, a query type keyed by another field). Add InternalId to the import.",
        "body": [
            "/** The internal id when it is not the property `id`: queried as ${2}, written through ${1}. */",
            "@InternalId()",
            "@Field('${1:line}', { queryFieldId: '${2:id}' })",
            "${3:lineId}!: number;"
        ]
    },
    "nspModelParentId": {
        "scope": "typescript",
        "prefix": "nspModelParentId",
        "description": "On a line or child class: the property holding the parent record's internal id, the field the rows are joined through. Add ParentId and ReadOnly to the import.",
        "body": [
            "/** The parent record's internal id: the field the rows are joined through. */",
            "@ParentId()",
            "@Field('${1:transaction}')",
            "@ReadOnly()",
            "${2:transactionId}!: number;"
        ]
    },
    "nspModelReference": {
        "scope": "typescript",
        "prefix": "nspModelReference",
        "description": "A reference to another record: the select field named <reference>Id, and the reference typed as a Pick of the target (its projection). Import the target type: import type { X } from './X'.",
        "body": [
            "/** The select field behind the reference, named <reference>Id so no @Reference() is needed. */",
            "@Field('${1:entity}')",
            "${2:customer}Id!: number;",
            "/** The reference; the Pick is its projection. @Reference('${2}Id', { targetKey, load: 'separate', join: 'auto' }) for a match on another field or a join N/query lacks. */",
            "${2}?: Pick<${3:Customer}, '${4:id}' | '${5:companyName}'>;"
        ]
    },
    "nspModelSubrecord": {
        "scope": "typescript",
        "prefix": "nspModelSubrecord",
        "description": "A subrecord property; the class is a plain class (nspModelSubrecordClass). clearListField names the list field cleared before an edit. Add Subrecord to the import and import the class.",
        "body": [
            "/** ${3:What it holds}; the list field is cleared before an edit. */",
            "@Subrecord({ clearListField: '${1:shipaddresslist}' })",
            "${2:shippingAddress}!: ${4:TransactionAddress};"
        ]
    },
    "nspModelSublist": {
        "scope": "typescript",
        "prefix": "nspModelSublist",
        "description": "A sublist property. The line class is a record type like any other: nspModel with nspModelInternalId (the key is `line`, queried as `id`) and nspModelParentId (the field the lines are joined through). queryType and relationship say how the lines are reached when the owner's root has no join to them. Add Sublist to the import and import the line class.",
        "body": [
            "/** The ${1} lines, reached from the ${2} root through ${3}, without the header line. */",
            "@Sublist('${1:item}', { queryType: '${2:transaction}', relationship: '${3:transactionlines}', filter: [{ fieldId: 'mainline', operator: 'IS', values: [false] }] })",
            "${4:lines}!: ${5:TransactionLine}[];"
        ]
    },
    "nspModelTransform": {
        "scope": "typescript",
        "prefix": "nspModelTransform",
        "description": "A property transformed on every read by an exported function of the same file (the build step imports it by name). Add Transform to the import.",
        "body": [
            "/** Transformed on every read by ${1}: `export function ${1}(value: unknown): ${3} { ... }` in this file. */",
            "@Transform(${1:trimText})",
            "@Field('${2:otherrefnum}')",
            "${4:poNumber}!: ${3:string} | null;"
        ]
    },
    "nspModelNotMapped": {
        "scope": "typescript",
        "prefix": "nspModelNotMapped",
        "description": "A property that is not a field: it stays on the type for a value filled in after the query. Add NotMapped to the import.",
        "body": [
            "/** Not a field: stays on the type for a value filled in after the query. */",
            "@NotMapped()",
            "${1:displayLabel}?: ${2:string};"
        ]
    },
    "nspModelSetFirst": {
        "scope": "typescript",
        "prefix": "nspModelSetFirst",
        "description": "A field written before the others, because NetSuite recomputes dependants when it changes. Add SetFirst to the import.",
        "body": [
            "/** Written before the other fields, because NetSuite recomputes dependants when it changes. */",
            "@SetFirst()",
            "${1:state}!: ${2:string | null};"
        ]
    },
    "nspModelExcludeFromDefaultSelect": {
        "scope": "typescript",
        "prefix": "nspModelExcludeFromDefaultSelect",
        "description": "A field left out of the default select; include() brings it back for one query. Add ExcludeFromDefaultSelect to the import.",
        "body": [
            "/** Left out of the default select; include() brings it back for one query. */",
            "@ExcludeFromDefaultSelect()",
            "@Field('${1:custbody_notes}')",
            "${2:notes}!: ${3:string | null};"
        ]
    },
    "nspRepositoryLog": {
        "scope": "typescript",
        "prefix": [
            "nspRepositoryLog",
            "nspLog"
        ],
        "description": "A log line in the shape the lint rule wants: a fixed title, the ids in the details object. Needs `import * as log from 'N/log'`, so a repository or _host file.",
        "body": [
            "log.${1|debug,audit,error|}('${2:what happened}', { ${3:id} });"
        ]
    },
    "nspNetsuiteIds": {
        "scope": "typescript",
        "prefix": [
            "nspNetsuiteIds",
            "nspAppIds"
        ],
        "description": "One more export in netsuite.ts: ids no controller or model owns (a saved search, a list value, a script parameter). Exported constants with `as const`, camelCase keys.",
        "body": [
            "/** ${1:What these ids reach}. */",
            "export const ${2:savedSearches} = {",
            "    ${3:key}: '${4:customsearch_x}',",
            "} as const;",
            ""
        ]
    },
    "nspTestController": {
        "scope": "typescript",
        "prefix": "nspTestController",
        "description": "api/__tests__/controllers/<name>Controller.test.ts: the endpoint against a mocked service (vi.hoisted, then vi.mock, then the import). The controller name comes from the file name.",
        "body": [
            "import { beforeEach, describe, expect, it, vi } from 'vitest';",
            "import type { ${1:ModelSummary} } from '../../src/services/${2:${TM_FILENAME_BASE/(.*)Controller\\.test$/$1/}}Service';",
            "",
            "// The controller is tested against a mocked service, so the test sees only what the endpoint does with the wire.",
            "const { ${3:serviceFunction} } = vi.hoisted(() => ({",
            "    ${3}: vi.fn<(${4:id}: ${5:number}) => ${1}[]>(),",
            "}));",
            "vi.mock('../../src/services/${2}Service', () => ({ ${3} }));",
            "",
            "import { ${TM_FILENAME_BASE/(.*)Controller\\.test$/$1/}Endpoints } from '../../src/controllers/${TM_FILENAME_BASE/(.*)Controller\\.test$/$1/}Controller';",
            "",
            "describe('${TM_FILENAME_BASE/(.*)Controller\\.test$/$1/}.${6:byId}', () => {",
            "    beforeEach(() => {",
            "        ${3}.mockReset();",
            "        ${3}.mockReturnValue([${7:{ id: 1 \\}}]);",
            "    });",
            "",
            "    it('unpacks the request, calls the service with it and shapes the reply', () => {",
            "        expect(${TM_FILENAME_BASE/(.*)Controller\\.test$/$1/}Endpoints.${6}({ ${4}: ${8:7} })).toEqual({ ${4}: ${8}, ${9:items}: [${7}] });",
            "        expect(${3}).toHaveBeenCalledWith(${8});",
            "    });",
            "});",
            ""
        ]
    },
    "nspTestService": {
        "scope": "typescript",
        "prefix": "nspTestService",
        "description": "api/__tests__/services/<subject>Service.test.ts: the service against a mocked repository. The subject comes from the file name.",
        "body": [
            "import { beforeEach, describe, expect, it, vi } from 'vitest';",
            "import type { ${1:Model} } from '../../src/types/models.gen';",
            "",
            "// The service is tested against a mocked repositories layer, so the test sees only the service's decisions.",
            "const { ${2:repositoryFunction} } = vi.hoisted(() => ({",
            "    ${2}: vi.fn<(${3:id}: ${4:number}) => ${1}[]>(),",
            "}));",
            "vi.mock('../../src/repositories/${5:${TM_FILENAME_BASE/(.*)Service\\.test$/$1/}}Repository', () => ({ ${2} }));",
            "",
            "import { build${1}Summary, ${6:serviceFunction} } from '../../src/services/${TM_FILENAME_BASE/(.*)Service\\.test$/$1/}Service';",
            "",
            "describe('build${1}Summary', () => {",
            "    it('keeps ${7:the id} only', () => {",
            "        expect(build${1}Summary(${8:{ id: 1 \\}})).toEqual(${9:{ id: 1 \\}});",
            "    });",
            "});",
            "",
            "describe('${6}', () => {",
            "    beforeEach(() => {",
            "        ${2}.mockReset();",
            "        ${2}.mockReturnValue([${8}]);",
            "    });",
            "",
            "    it('queries by ${3} and shapes every row', () => {",
            "        expect(${6}(${10:7})).toEqual([${9}]);",
            "        expect(${2}).toHaveBeenCalledWith(${10});",
            "    });",
            "});",
            ""
        ]
    },
    "nspTestRepository": {
        "scope": "typescript",
        "prefix": "nspTestRepository",
        "description": "api/__tests__/repositories/<set>Repository.test.ts: the repository against a fake dbContext whose set records the specifications applied to it. Set and model names come from the file name.",
        "body": [
            "import { beforeEach, describe, expect, it, vi } from 'vitest';",
            "import type { ${1:${TM_FILENAME_BASE/^(.)|(ies)Repository\\.test$|(ss|x|z|ch|sh)esRepository\\.test$|s?Repository\\.test$/${1:/upcase}${2:+y}$3/g}} } from '../../src/types/models.gen';",
            "",
            "// The repository is tested against a fake dbContext carrying only the ${2:${TM_FILENAME_BASE/(.*)Repository\\.test$/$1/}} set, whose list()",
            "// records the specifications applied to it.",
            "const { fake${2/^(.)/${1:/upcase}/} } = vi.hoisted(() => ({ fake${2/^(.)/${1:/upcase}/}: { list: vi.fn() } }));",
            "vi.mock('../../src/repositories/generated/context.gen', () => ({ dbContext: { ${2}: fake${2/^(.)/${1:/upcase}/} } }));",
            "",
            "import { ${3:repositoryFunction} } from '../../src/repositories/${TM_FILENAME_BASE/(.*)Repository\\.test$/$1/}Repository';",
            "",
            "/** Records the query builder calls a specification makes, so a test can assert on them. */",
            "function createRecordingQuery() {",
            "    const calls: Array<{ method: string; args: unknown[] }> = [];",
            "    const query: Record<string, (...args: unknown[]) => unknown> = {};",
            "    for (const method of ['where', 'orderByAsc', 'orderByDesc', 'page']) {",
            "        query[method] = (...args: unknown[]) => {",
            "            calls.push({ method, args });",
            "            return query;",
            "        };",
            "    }",
            "    return { query, calls };",
            "}",
            "",
            "/** Makes the fake set's list() apply every specification to a recording query and return the rows. */",
            "function listReturning(rows: ${1}[]) {",
            "    const recording = createRecordingQuery();",
            "    fake${2/^(.)/${1:/upcase}/}.list.mockImplementation((...specifications: Array<(query: unknown) => unknown>) => {",
            "        specifications.forEach((specification) => specification(recording.query));",
            "        return rows;",
            "    });",
            "    return recording.calls;",
            "}",
            "",
            "beforeEach(() => {",
            "    fake${2/^(.)/${1:/upcase}/}.list.mockReset();",
            "});",
            "",
            "describe('${3}', () => {",
            "    const rows: ${1}[] = ${4:[]};",
            "",
            "    it('returns the rows the set lists', () => {",
            "        listReturning(rows);",
            "        expect(${3}(${5:7})).toBe(rows);",
            "    });",
            "",
            "    it('filters the set to ${6:the id} and nothing else', () => {",
            "        const calls = listReturning(rows);",
            "        ${3}(${5});",
            "        expect(calls).toEqual([{ method: 'where', args: ['${7:id}', '=', ${5}] }]);",
            "    });",
            "});",
            ""
        ]
    },
    "nspTestRepositorySuitelet": {
        "scope": "typescript",
        "prefix": "nspTestRepositorySuitelet",
        "description": "api/__tests__/repositories/<name>Repository.test.ts for a repository that is a Suitelet client: a fake client, and the script it is built for. The controller name comes from the file name.",
        "body": [
            "import { beforeEach, describe, expect, it, vi } from 'vitest';",
            "import { scripts } from '../../src/scripts.gen';",
            "",
            "// The repository is tested against a fake Suitelet client: what it asks the ${1:${TM_FILENAME_BASE/(.*)Repository\\.test$/$1/}} script for, not how the wire looks.",
            "const { ${2:byId}, createSuiteletClient } = vi.hoisted(() => {",
            "    const ${2} = vi.fn();",
            "    return { ${2}, createSuiteletClient: vi.fn(() => ({ ${2} })) };",
            "});",
            "vi.mock('@amerilux/netsuite-api/server', () => ({ createSuiteletClient }));",
            "",
            "import { ${3:repositoryFunction} } from '../../src/repositories/${1}Repository';",
            "",
            "// The client is built when the module loads, before any test runs, and mock state is cleared per test: keep the call.",
            "const clientConstructionArguments = createSuiteletClient.mock.calls[0] as unknown[] | undefined;",
            "",
            "beforeEach(() => {",
            "    ${2}.mockReset();",
            "});",
            "",
            "describe('${3}', () => {",
            "    it('builds the client for the ${1} script', () => {",
            "        expect(clientConstructionArguments).toEqual([scripts.${1}]);",
            "    });",
            "",
            "    it('asks the Suitelet for the ${4:id} and returns its ${5:items}', () => {",
            "        const ${5} = ${6:[]};",
            "        ${2}.mockReturnValue({ ${4}: ${7:7}, ${5} });",
            "        expect(${3}(${7})).toBe(${5});",
            "        expect(${2}).toHaveBeenCalledWith({ ${4}: ${7} });",
            "    });",
            "});",
            ""
        ]
    },
    "nspTestHook": {
        "scope": "typescript",
        "prefix": "nspTestHook",
        "description": "client/__tests__/<controller>Query.test.ts: the generated client and a query hook's options against a fake api client. Written for a hook with an argument (nspHookQueryWith); for one without, drop the argument and compare the key directly.",
        "body": [
            "import { describe, expect, it, vi } from 'vitest';",
            "",
            "// The hook is tested against a fake ${1:${TM_FILENAME_BASE/(.*)Query\\.test$/$1/}} client: what it asks for, not how the wire looks.",
            "const { ${2:byId}Mock, createApiClient } = vi.hoisted(() => {",
            "    const ${2}Mock = vi.fn(async () => (${3:{ id: 7 \\}}));",
            "    return { ${2}Mock, createApiClient: vi.fn(() => ({ ${2}: ${2}Mock })) };",
            "});",
            "vi.mock('@amerilux/netsuite-api/client', () => ({ createApiClient }));",
            "",
            "import { ${1} } from '@/api/index.gen';",
            "import { ${4:hookName}QueryKey, ${4}QueryOptions } from '@/hooks/use${4/^(.)/${1:/upcase}/}';",
            "",
            "// The generated modules build their clients when they load, before any test runs, and mock state is cleared per test: keep the calls.",
            "const constructedScripts = (createApiClient.mock.calls as unknown[][]).map((call) => call[0]);",
            "",
            "describe('${1}.api', () => {",
            "    it('is the typed client for the script the ${1} controller declares, as npm run generate wrote it', () => {",
            "        expect(constructedScripts).toContainEqual({ kind: '${5|restlet,suitelet|}', scriptId: 'customscript_{{prefix}}_${6:${TM_FILENAME_BASE/(?:Query\\.test$)|([A-Z])/${1:+_}${1:/downcase}/g}}', deployId: 'customdeploy_{{prefix}}_${6}' });",
            "        expect(${1}.api.${2}).toBe(${2}Mock);",
            "    });",
            "});",
            "",
            "describe('${4}QueryOptions', () => {",
            "    it('binds the ${2} endpoint under its key and forwards the abort signal', async () => {",
            "        const options = ${4}QueryOptions(${7:7});",
            "        expect(options.queryKey).toEqual(${4}QueryKey(${7}));",
            "        const signal = new AbortController().signal;",
            "        const queryFunction = options.queryFn as (context: { signal: AbortSignal }) => Promise<unknown>;",
            "        await expect(queryFunction({ signal })).resolves.toEqual(${3});",
            "        expect(${2}Mock).toHaveBeenCalledWith(${8:{ id: ${7} \\}}, { signal });",
            "    });",
            "});",
            ""
        ]
    },
    "nspHookQuery": {
        "scope": "typescript",
        "prefix": [
            "nspHookQuery",
            "nspHook"
        ],
        "description": "client/src/hooks/use<Name>.ts: a query hook for an endpoint that takes no request; names come from the file name. For an endpoint with a request use nspHookQueryWith.",
        "body": [
            "import { queryOptions, useQuery } from '@tanstack/react-query';",
            "import { ${1:controller} } from '@/api/index.gen';",
            "",
            "export const ${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryKey = ['${1}', '${2:endpoint}'] as const;",
            "",
            "export function ${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryOptions() {",
            "    return queryOptions({",
            "        queryKey: ${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryKey,",
            "        // The endpoint takes no request; the second argument carries the abort signal.",
            "        queryFn: ({ signal }) => ${1}.api.${2}(undefined, { signal }),",
            "    });",
            "}",
            "",
            "/** ${3:What it answers}. */",
            "export function ${TM_FILENAME_BASE}() {",
            "    return useQuery(${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryOptions());",
            "}",
            ""
        ]
    },
    "nspHookQueryWith": {
        "scope": "typescript",
        "prefix": [
            "nspHookQueryWith",
            "nspHookWith"
        ],
        "description": "client/src/hooks/use<Name>.ts: a query hook for an endpoint that takes a request; the argument is part of the key. Names come from the file name. Next: nspTestHook, nspPage.",
        "body": [
            "import { queryOptions, useQuery } from '@tanstack/react-query';",
            "import { ${1:controller} } from '@/api/index.gen';",
            "",
            "export const ${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryKey = (${3:id}: ${4:number}) => ['${1}', '${2:endpoint}', ${3}] as const;",
            "",
            "export function ${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryOptions(${3}: ${4}) {",
            "    return queryOptions({",
            "        queryKey: ${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryKey(${3}),",
            "        queryFn: ({ signal }) => ${1}.api.${2}({ ${3} }, { signal }),",
            "    });",
            "}",
            "",
            "/** ${5:What it answers}. */",
            "export function ${TM_FILENAME_BASE}(${3}: ${4}) {",
            "    return useQuery(${TM_FILENAME_BASE/^use(.)(.*)$/${1:/downcase}$2/}QueryOptions(${3}));",
            "}",
            ""
        ]
    },
    "nspHookMutation": {
        "scope": "typescript",
        "prefix": [
            "nspHookMutation",
            "nspMutation"
        ],
        "description": "client/src/hooks/use<Verb><Name>.ts: a mutation hook calling one endpoint, then refetching every query of that controller (their keys start with its name). The hook name comes from the file name.",
        "body": [
            "import { useMutation, useQueryClient } from '@tanstack/react-query';",
            "import { ${1:controller} } from '@/api/index.gen';",
            "",
            "/** ${3:What it changes}. Every query of the ${1} controller is refetched afterwards. */",
            "export function ${TM_FILENAME_BASE}() {",
            "    const queryClient = useQueryClient();",
            "    return useMutation({",
            "        mutationFn: (request: ${1}.${2/^(.)/${1:/upcase}/}Request) => ${1}.api.${2:create}(request),",
            "        onSuccess: () => queryClient.invalidateQueries({ queryKey: ['${1}'] }),",
            "    });",
            "}",
            ""
        ]
    },
    "nspPage": {
        "scope": "typescriptreact",
        "prefix": "nspPage",
        "description": "client/src/pages/<Name>Page.tsx: a page calls a hook; only a hook calls the api. The component name comes from the file name. Next: nspRoute in client/src/routes.",
        "body": [
            "import { ${1:useHook} } from '@/hooks/${1}';",
            "",
            "/**",
            " * ${2:What the page shows}. A page calls a hook; only a hook calls the api. A failed call is reported to the",
            " * AppShell's banner before the query sees it; a page that shows the failure in place reads isError instead.",
            " */",
            "export function ${TM_FILENAME_BASE}() {",
            "    const ${3:${1/^use(.)(.*)$/${1:/downcase}$2/}} = ${1}(${4});",
            "",
            "    return (",
            "        <section className=\"flex flex-col gap-4\">",
            "            <h2 className=\"text-lg font-semibold text-slate-900\">${5:Title}</h2>",
            "",
            "            {${3}.isPending && <p className=\"text-sm text-slate-500\">Loading…</p>}",
            "",
            "            {${3}.isSuccess && (",
            "                <pre className=\"text-sm text-slate-700\">{JSON.stringify(${3}.data, null, 2)}</pre>$0",
            "            )}",
            "        </section>",
            "    );",
            "}",
            ""
        ]
    },
    "nspRoute": {
        "scope": "typescriptreact",
        "prefix": "nspRoute",
        "description": "client/src/routes/<segment>.tsx: a file-based route. The path comes from the file name (orders.tsx is /orders, orders.$orderId.tsx is /orders/$orderId, index.tsx is /); the Vite plugin regenerates routeTree.gen.ts.",
        "body": [
            "import { createFileRoute } from '@tanstack/react-router';",
            "import { ${1:${TM_FILENAME_BASE/^(.)(.*)$/${1:/upcase}$2/}Page} } from '@/pages/${1}';",
            "",
            "export const Route = createFileRoute('${TM_FILENAME_BASE/^index$|^|\\./\\//g}')({",
            "    component: ${1},",
            "});",
            ""
        ]
    },
    "nspObjectRestlet": {
        "scope": "xml",
        "prefix": [
            "nspObjectRestlet",
            "nspSdfRestlet"
        ],
        "description": "netsuite/Objects/customscript_{{prefix}}_<snake_name>.xml for a Restlet controller; ids and the script file come from the file name. Next: npm run generate.",
        "body": [
            "<restlet scriptid=\"${TM_FILENAME_BASE}\">",
            "  <description>${2:What it serves}, for {{appTitle}}.</description>",
            "  <isinactive>F</isinactive>",
            "  <name>{{appTitle}} ${1:Name}</name>",
            "  <notifyadmins>F</notifyadmins>",
            "  <notifyemails></notifyemails>",
            "  <notifyowner>T</notifyowner>",
            "  <notifyuser>F</notifyuser>",
            "  <scriptfile>[/SuiteScripts/{{appName}}/api/controllers/${TM_FILENAME_BASE/^customscript_{{prefix}}_|_([a-z])/${1:/upcase}/g}Controller.js]</scriptfile>",
            "  <scriptdeployments>",
            "    <scriptdeployment scriptid=\"${TM_FILENAME_BASE/^customscript_/customdeploy_/}\">",
            "      <allemployees>F</allemployees>",
            "      <allpartners>F</allpartners>",
            "      <allroles>F</allroles>",
            "      <audslctrole>${3:ADMINISTRATOR}</audslctrole>",
            "      <isdeployed>T</isdeployed>",
            "      <loglevel>DEBUG</loglevel>",
            "      <status>RELEASED</status>",
            "      <title>{{appTitle}} ${1}</title>",
            "    </scriptdeployment>",
            "  </scriptdeployments>",
            "</restlet>",
            ""
        ]
    },
    "nspObjectSuitelet": {
        "scope": "xml",
        "prefix": [
            "nspObjectSuitelet",
            "nspSdfSuitelet"
        ],
        "description": "netsuite/Objects/customscript_{{prefix}}_<snake_name>.xml for a Suitelet controller; ids and the script file come from the file name. Remove <runasrole> unless the script must run as another role. Next: npm run generate.",
        "body": [
            "<suitelet scriptid=\"${TM_FILENAME_BASE}\">",
            "  <description>${2:What it serves}, for {{appTitle}}.</description>",
            "  <isinactive>F</isinactive>",
            "  <name>{{appTitle}} ${1:Name}</name>",
            "  <notifyadmins>F</notifyadmins>",
            "  <notifyemails></notifyemails>",
            "  <notifyowner>T</notifyowner>",
            "  <notifyuser>F</notifyuser>",
            "  <scriptfile>[/SuiteScripts/{{appName}}/api/controllers/${TM_FILENAME_BASE/^customscript_{{prefix}}_|_([a-z])/${1:/upcase}/g}Controller.js]</scriptfile>",
            "  <scriptdeployments>",
            "    <scriptdeployment scriptid=\"${TM_FILENAME_BASE/^customscript_/customdeploy_/}\">",
            "      <allemployees>F</allemployees>",
            "      <allpartners>F</allpartners>",
            "      <allroles>T</allroles>",
            "      <audslctrole></audslctrole>",
            "      <eventtype></eventtype>",
            "      <isdeployed>T</isdeployed>",
            "      <isonline>F</isonline>",
            "      <loglevel>DEBUG</loglevel>",
            "      ${3:<runasrole>ADMINISTRATOR</runasrole>}",
            "      <status>RELEASED</status>",
            "      <title>{{appTitle}} ${1}</title>",
            "    </scriptdeployment>",
            "  </scriptdeployments>",
            "</suitelet>",
            ""
        ]
    },
    "nspObjectMapReduce": {
        "scope": "xml",
        "prefix": [
            "nspObjectMapReduce",
            "nspSdfMapReduce"
        ],
        "description": "netsuite/Objects/customscript_{{prefix}}_<snake_name>_mr.xml for a job: its parameters and one deployment. A second deployment (the same id plus _2) lets two runs of the job overlap. Next: npm run generate.",
        "body": [
            "<mapreducescript scriptid=\"${TM_FILENAME_BASE}\">",
            "  <description>${2:What a run of it does}, for {{appTitle}}.</description>",
            "  <isinactive>F</isinactive>",
            "  <name>{{appTitle}} ${1:Name}</name>",
            "  <notifyadmins>F</notifyadmins>",
            "  <notifyemails></notifyemails>",
            "  <notifyowner>T</notifyowner>",
            "  <scriptfile>[/SuiteScripts/{{appName}}/api/jobs/${TM_FILENAME_BASE/^customscript_{{prefix}}_|_mr$|_([a-z])/${1:/upcase}/g}/${TM_FILENAME_BASE/^customscript_{{prefix}}_|_mr$|_([a-z])/${1:/upcase}/g}.js]</scriptfile>",
            "  <scriptcustomfields>",
            "    <scriptcustomfield scriptid=\"${TM_FILENAME_BASE/^customscript_(.*)_mr$/custscript_$1_run/}\">",
            "      <accesslevel>2</accesslevel>",
            "      <aidescription></aidescription>",
            "      <applyformatting>F</applyformatting>",
            "      <checkspelling>F</checkspelling>",
            "      <defaultchecked>F</defaultchecked>",
            "      <defaultselection></defaultselection>",
            "      <defaultvalue></defaultvalue>",
            "      <description>The run record this run reports to. Left empty: a scheduled run opens its own.</description>",
            "      <displayheight></displayheight>",
            "      <displaytype>NORMAL</displaytype>",
            "      <displaywidth></displaywidth>",
            "      <dynamicdefault></dynamicdefault>",
            "      <fieldtype>TEXT</fieldtype>",
            "      <isformula>F</isformula>",
            "      <ismandatory>F</ismandatory>",
            "      <label>Run Id</label>",
            "      <linktext></linktext>",
            "      <maxlength></maxlength>",
            "      <maxvalue></maxvalue>",
            "      <minvalue></minvalue>",
            "      <onparentdelete></onparentdelete>",
            "      <searchlevel>2</searchlevel>",
            "      <selectrecordtype></selectrecordtype>",
            "      <setting></setting>",
            "      <storevalue>T</storevalue>",
            "    </scriptcustomfield>",
            "  </scriptcustomfields>",
            "  <scriptdeployments>",
            "    <scriptdeployment scriptid=\"${TM_FILENAME_BASE/^customscript_/customdeploy_/}\">",
            "      <buffersize>1</buffersize>",
            "      <concurrencylimit>1</concurrencylimit>",
            "      <isdeployed>T</isdeployed>",
            "      <loglevel>AUDIT</loglevel>",
            "      <queueallstagesatonce>F</queueallstagesatonce>",
            "      <runasrole>${3:ADMINISTRATOR}</runasrole>",
            "      <status>NOTSCHEDULED</status>",
            "      <title>{{appTitle}} ${1}</title>",
            "      <yieldaftermins>60</yieldaftermins>",
            "    </scriptdeployment>",
            "  </scriptdeployments>",
            "</mapreducescript>",
            ""
        ]
    }
}
